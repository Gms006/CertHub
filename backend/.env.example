# API
ENV=dev
API_HOST=0.0.0.0
API_PORT=8010

# DB
DATABASE_URL=postgresql+psycopg2://certhub:619038@127.0.0.1:5433/certhub?client_encoding=utf8

# Redis/RQ
REDIS_URL=redis://localhost:6380/0
RQ_DEFAULT_QUEUE=default

# Multi-tenant (se já tiver org_id/GUC no seu core)
DEFAULT_ORG_ID=1

# Catálogo de certificados (catálogo/ingest)
CERTS_ROOT_PATH=SEU_DIRETORIO_DE_CERTIFICADOS_AQUI
OPENSSL_PATH=C:\Program Files\OpenSSL-Win64\bin\openssl.exe

# JWT (S2)
JWT_SECRET=SEU_SEGREDO_JWT_AQUI
ACCESS_TOKEN_TTL_MIN=30
DEVICE_TOKEN_TTL_MIN=10
REFRESH_TTL_DAYS=14
SET_PASSWORD_TOKEN_TTL_MIN=10
RESET_PASSWORD_TOKEN_TTL_MIN=30
BCRYPT_COST=12
LOCKOUT_MAX_ATTEMPTS=5
LOCKOUT_MINUTES=15
COOKIE_SECURE=true
COOKIE_SAMESITE=Strict
COOKIE_HTTPONLY=true
ALLOW_LEGACY_HEADERS=true

# Agent (S4+)
AGENT_API_KEY=CHANGE_ME_LONG_RANDOM

# Email (S5)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=certificados.netocontabilidade@gmail.com
SMTP_PASS=SUA_SENHA_DE_APP_AQUI
SMTP_FROM="CertHub <certificados.netocontabilidade@gmail.com>"

FRONTEND_BASE_URL=http://localhost:5173